[project]
name = "final"
version = "0.1.0"
description = "Final Project PKMS + Task Manager"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.8.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[project.scripts]
final = "main:main"
